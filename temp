leetcode problem no 1920 
	sol 
	
	vector<int> buildArray(vector<int>& nums) {
        int n = nums.size();
        vector<int> temp;
        for(int i=0; i<n; i++) {
            temp[i] = nums[nums[i]];
        }
        return temp;
    }
